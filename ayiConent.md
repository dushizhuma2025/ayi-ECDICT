# ECDICT Word Explorer 产品逻辑文档

本文档持续记录并更新 ECDICT Word Explorer 项目的产品逻辑、流程逻辑和技术实现逻辑。

## 1. 系统概述
ECDICT Word Explorer 是基于 ECDICT（自由英汉词典数据库）开发的离线单词一览与多维检索系统。
- **数据层**：启动时自动将 `ecdict.csv` 转存并创建 SQLite 本地数据库 `ecdict.db`，同时针对 `collins`、`oxford`、`tag`、`bnc`、`frq` 字段建立快速查询索引。
- **后端层**：纯 Python 零依赖架构。利用 Python 内置的 `http.server` 托管静态页面并提供 RESTful 查询接口。
- **前端层**：使用 Vanilla HTML5/CSS3/JS 搭建单页面 Web 应用（SPA），采用 Glassmorphism 暗黑毛玻璃风格与响应式表格。

---

## 2. 核心功能逻辑

### 2.1 单词一览与多维检索
- **实时前缀匹配**：支持输入单词前缀在数十万条数据中进行前缀匹配搜索。
- **标签筛选**：支持中考（zk）、高考（gk）、四级（cet4）、六级（cet6）、考研（ky）、托福（toefl）、雅思（ielts）、GRE（gre）等考纲词汇筛选。
- **柯林斯星级筛选**：支持选择“全部”、“五星”、“四星以上”、“三星以上”、“二星以上”、“一星以上”以及“未评级”来过滤单词，后端采用大于等于（>=）的方式进行范围匹配（对于“未评级”，匹配 0 星或为空的单词；“全部”不加限制）。
- **牛津核心词筛选**：支持单独勾选过滤牛津 3000 核心词汇。
- **词频范围过滤**：支持输入 BNC 与 COCA 词频排名的上限与下限区间进行精准过滤。
- **分页组件**：支持自定义每页显示条数（10、20、50、100），并能计算展示动态页码，支持直接输入页码跳转。
- **详情侧边抽屉**：点击任意单词行，从右侧滑出抽屉面板，以完整版大字号展示音标（不含中括号）、中文释义换行、英文双解释义、时态变形和精确词频。
- **智能词性填补**：当数据库中的 `pos` 字段为空时，系统会自动解析 `translation`（中文释义）中的每行文本。若某行以小写英文字符后接点号开头（如 `n.`, `adj.`, `vt.`），则自动提取该标识作为词性，多词性通过 `/` 进行去重拼接（如 `n/a`）。该补全逻辑在网页展示与导出 CSV 时同步生效。
- **表头字段排序**：支持点击柯林斯（Collins）、BNC、COCA 表头进行排序切换。为了确保高频词优先展示：
  - 柯林斯星级按**降序（DESC）**排序（5星在前，0星在后）；
  - BNC 与 COCA（词频排名）按**升序（ASC）**正序排序（数值越小代表词频越高，1在前，大数值在后），且在排序时优先把有排名的数据排在前面，未排名的排在最后。

---

## 3. 数据导出功能逻辑

### 3.1 导出入口与操作流程
- 在前端一览页面的右上角（页面头部）设有一个醒目的 **“导出 CSV”** 按钮。
- 用户点击该按钮时，前端会捕获**当前页面上所有的筛选条件和搜索关键词**。
- 前端将这些查询参数发送给后端的导出专用接口，后端计算所有符合过滤条件的记录并直接流式生成 CSV 文件返回给浏览器下载。

### 3.2 导出 CSV 数据格式规格
导出的 CSV 格式须严格遵照以下标头及映射清洗逻辑：

| 标头名称 | 数据字段映射与逻辑 |
|---|---|
| **所在单元** | 整数类型。按照导出单词的生成顺序（1-based 序号 `i`），每 10 个单词划分为一个单元。计算公式：`(i - 1) // 10 + 1`。（例如：第 1-10 个单词对应单元 `1`；第 11-20 个单词对应单元 `2`）。 |
| **词条** | 映射数据库 `word` 字段。 |
| **音标** | 映射数据库 `phonetic` 字段。写入时**剔除首尾的 `[` 与 `]` 中括号**（例如 `[hʊd]` 清洗为 `hʊd`）。 |
| **词性** | 映射数据库 `pos` 字段（如 `n:40/v:60`，若为空则自动通过中文释义匹配填补，例如 `n/a`）。 |
| **英文释义** | 映射数据库 `definition` 字段。 |
| **中文释义** | 映射数据库 `translation` 字段（保留其内嵌换行）。 |
| **例句** | 由于基础库例句字段缺失，该列直接**保持空白**。 |
| **例句翻译** | 由于基础库例句翻译字段缺失，该列直接**保持空白**。 |

### 3.3 技术实现逻辑
- **后端导出接口**：路由为 `GET /api/words/export`。
- **参数传递**：接收与 `/api/words` 完全一致的检索 Query 过滤参数，但不加 `limit` 与 `page` 分页约束，以查询出所有符合当前筛选的全部单词。
- **数据防暴增策略**：为保证本地 Python 服务的响应稳定性，当查询到的总词条数超过 5,000 条时，后端将在生成的 CSV 中只截取前 5,000 条，并在返回的 HTTP 头部或首行加入限制标记，防止在未输入任何筛选条件时因误触导出导致服务器内存溢出。
- **下载触发**：前端通过构建动态 `<a>` 链接并触发 `click()`，将参数拼接在链接后直接请求后端实现文件附带下载。
